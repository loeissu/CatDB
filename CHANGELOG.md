# Changelog

## v2.1.0 (2026-09-03)

### 🐛 Bug 修复

| 问题 | 严重程度 | 修复内容 |
|------|----------|----------|
| `console=True` 导致打包后显示黑色控制台窗口 | 中 | 改为 `console=False`，系统托盘应用不再显示控制台 |
| `requirements.txt` 缺少 pystray/netifaces/qrcode/Pillow | 高 | 补全所有依赖声明 |
| 缺少 `/qr` 二维码路由 | 中 | 新增 QR 码生成接口 |
| 缺少 Zeroconf 服务发现 | 中 | 新增局域网自动发现注册 |
| `compute_diff` 只比较前缀，编辑中间内容会出错 | 高 | 重写 diff 算法，支持前后缀匹配 |
| 缺少 `import logging` | 中 | 添加日志模块导入 |
| 缺少 `argparse` 命令行参数支持 | 中 | 新增 `--port` 和 `--hotkey` 参数 |

### ✨ 新增功能

| 功能 | 说明 |
|------|------|
| **系统托盘** | 启动后最小化到托盘，右键菜单显示二维码/退出 |
| **10秒超时自动弹窗** | 启动后等待手机连接，10秒无连接自动弹出二维码窗口 |
| **搜索状态显示** | 托盘图标显示"搜索中"/"已连接"/"等待扫码"状态 |
| **QR 码窗口** | 独立窗口显示二维码和连接地址，扫码即可连接 |
| **Zeroconf 自动发现** | 局域网内手机可自动发现服务，无需手动输入地址 |

### 🔄 桌面应用启动流程

```
双击启动 CatDB.exe
    ↓
显示系统托盘图标（状态：搜索中...）
    ↓
等待手机连接（最多10秒）
    ↓
┌─ 手机已连接 → 状态变为"已连接"，可正常使用
└─ 超时未连接 → 自动弹出二维码扫码窗口
                    ↓
                手机扫码 → 进入操作界面
                    ↓
                可最小化到托盘继续使用
```

### 📦 打包产物

| 平台 | 文件 | 大小 |
|------|------|------|
| Windows | `CatDB_v2.1.0_windows.exe` | ~107 MB |
| Android (Debug) | `catdb_v2.1.0_debug.apk` | ~3.6 MB |

### 📝 代码变更统计

- `server.py`: +85 行 / -15 行
- `requirements.txt`: +4 行依赖
- `豆包喵喵.spec`: `console=True` → `console=False`

---

## v2.0.0 (2026-09-02)

### 初始版本

- 系统托盘重构
- 日志迁移 `print()` → `logging`
- 双线程架构（主线程托盘 + 守护线程 Web）
- 命令行参数支持
- IP 智能过滤
- Zeroconf 服务注册
- 二维码路由 `/qr`
- 移动端 PWA 支持
- 全平台打包脚本（Windows/macOS/Linux）
