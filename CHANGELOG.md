# Changelog

## [2.2.4] - 2026-09-04

### Changed
- **安卓主页大幅简化**：移除主页的扫码连接条、地址栏与图片上传按钮；连接地址收进「设置」，点小猫头像=从相册选图发到电脑，右侧新增圆形拍照按钮（与左侧触控板按钮对称同款）
- **拍照按钮**：主页右下角圆形 📷 按钮直接调起相机/选图，拍到的照片经 WebSocket 粘贴到电脑光标处
- **触控板返回逻辑修复**：触控板页面新增「返回输入」按钮，可一键可靠回到输入界面（两个圆形按钮随之隐藏/恢复）
- **电脑地址输入**：在设置中填写即保存、重新打开不变，除非手动修改否则永不被覆盖
- **桌面端实时输入预览**：手机端打字时桌面窗口实时同步显示「手机正在输入：…」，停止输入数秒后自动消失
- **APP/EXE 图标**：改用上游原始项目的 6-phone-cat 图标（橙猫），同时应用于安卓 mipmap 全套与 Windows EXE
- 移除已废弃的扫码连接（jsQR）代码与依赖
- 版本号统一升至 2.2.4（versionCode 6）

## [2.2.3] - 2026-09-03

### Added
- **安卓深浅色主题**：设置中可切换 浅色 / 深色 / 跟随系统，默认跟随系统；页面背景、卡片、文字、输入框、小猫插画随主题适配，状态栏颜色与主题联动（不再是纯黑）
- **手机端扫码连接**：新增扫码入口，调起相机扫描电脑端二维码自动填入地址并连接；不可用时提示引导手动输入
- **手机图片发送到电脑**：输入框旁的图片按钮与点击中间小猫头像均可选择图片，通过 WebSocket 发送到电脑并直接粘贴到当前光标位置
- **小猫爪爪动画**：输入文字时中间小猫的两只爪爪随打字节奏摆动，停止输入后静止
- **APP 图标**：打包产物配有由原始小猫形象生成的图标（Android mipmap 全套 + Windows EXE）

### Fixed
- **安卓电脑地址输入框无法修改**：修复编辑失效问题，保存后立即重连并校验格式，错误时明确提示
- **桌面端端口耗尽会卡死**：启动/运行中自动检查端口占用并顺延；范围内全部被占时明确报错并记日志，不再静默卡死

### Changed
- **桌面界面进一步精简**：默认几乎不显示说明文字，提示改为悬停/点击气泡出现；日志面板可折叠
- 版本号统一升至 2.2.3（versionCode 5）

## [2.2.2] - 2026-09-03

### Fixed
- **桌面客户端无法操作 / 无日志输出**：pywebview 的 JS 桥接在页面加载后才注入，此前页面启动时误判为“浏览器模式”，导致所有操作与轮询失效。现在每次调用都实时探测桥接，并在 `pywebviewready` 事件后立即刷新
- **EXE 无法打开桌面窗口**：pywebview 自带 WebView2/互操作原生 DLL 此前未打入 EXE；spec 现通过 `collect_dynamic_libs('webview')` 显式收集
- **无声故障难排查**：日志同时写入 `%APPDATA%/CatDB/catdb.log`；服务线程异常不再吞掉，会记日志并立即返回；窗口创建失败自动降级为浏览器 + 托盘模式
- **显示/连接错误的 IP**：IP 列表私网地址优先排序，避免 VPN/虚拟网卡地址（如 198.18.x.x）抢在前面导致手机连不上
- **安卓状态栏黑色**：补装 `@capacitor/status-bar` 插件（此前配置完全没生效），状态栏固定 #F9F5F0 纯色 + 深色图标，与页面背景一致；页面背景统一为同色
- 版本号统一升至 2.2.2（versionCode 4）

### Changed
- 桌面界面按苹果设计语言重构：毛玻璃顶栏、卡片化三栏、macOS 风格开关/弹窗、系统字体栈、柔和阴影圆角

## [2.2.0] - 2026-09-03

### Added
- 桌面 GUI 主窗口（pywebview），920×660 三栏布局
- 触控板模式开关（左侧控制面板，对接上游已有后端）
- 开机自启控制（齿轮菜单，状态持久化）
- Android 纯色状态栏（与页面背景一致，不覆盖内容）
- GitHub Actions 自动构建 Windows EXE + Android Release APK
- HTTP JSON API（/api/status、/api/logs、/api/action），供浏览器版桌面页使用

### Fixed
- **EXE 打开 404 / 页面打不开**：GUI 改为加载本地打包的 desktop.html（不再猜测端口访问 127.0.0.1:5000），并新增端口自动探测；彻底移除协程内阻塞式 Event.wait（实测会饿死 Windows Proactor 事件循环导致全部 HTTP 超时）
- **APK 未签名无法安装**：release 构建类型固定使用 debug 签名（signingConfig signingConfigs.debug），CI 增加 apksigner 校验步骤
- **安卓 App 连不上电脑**：原 App 内置页面把 localhost 当成电脑地址；新增「电脑地址」手动填写（设置弹窗），并放行明文流量（usesCleartextTraffic）
- 移除 server.py 内嵌重复手机页（HTML_PAGE），统一以 www/index.html 为唯一事实来源

### Changed
- 主程序支持 `--minimized` 静默启动模式
- 桌面页重写：flex 流式三栏布局（25%/自适应/30%），状态灯、二维码 5s 自刷新、IP 标签切换、深色终端日志面板、齿轮菜单（开机自启/最小化/退出）
- 触控板开关真实生效（后端过滤手机触控指令）并持久化到 %APPDATA%/CatDB/config.json
- 快捷键可在桌面端修改（F1–F12/Home/End 等），运行中即时重建监听

### Removed
- 无（完整保留上游所有功能）