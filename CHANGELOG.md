# Changelog

## [2.2.11] - 2026-09-04

### Changed（启动器图标正式设计 + 状态栏主题即时生效）
- **启动器图标正式设计（替换 v2.2.10 橙色占位）**：采用与 Windows 桌面端/网页同体系的米白奶油渐变底（#FFFFFF→#FFF9F0→#F7EAD8，顶部主光源柔光 + 底部暖色暗角 + 细描边），居中深棕渐变粗体「Typing」（软投影 + 纵向渐变填充），下方橙色手绘笔触点缀——以「白纸 + 深棕字 + 橙笔」的排版意象呈现，48px 起各尺寸清晰可辨
- **全套图标按新设计重生成**：legacy（48/72/96/144/192）与 round 同母版，自适应前景收敛进 66% 安全区，自适应背景改纯奶油色 #FFF9F0（ic_launcher_background.xml 同步）
- **生成器统一为正式稿管道**：tools/gen_typing_icons.py 重构为方案 B 正式稿生成器（唯一权威入口）；新增矢量母稿 typing_icon.svg；generate_icons.js 不再各自渲染，统一委托 Python 生成器，任何入口重跑结果一致；cat_icon.svg / www/cat_icon.svg 保留（网页/PWA 猫图标仍引用）
- **状态栏随深浅主题即时生效（不再需要重启）**：JS 切换主题时同步通道双保险——首选原生 CatDBStatusBar 插件（WindowInsetsControllerCompat 直控），官方 StatusBar 插件始终作为第二通道兜底；修复兜底 setStyle 明暗映射反了的问题（深色主题应为浅色图标 style=DARK），并修复 Capacitor 配置里启动默认 style 与浅色底色不匹配（DARK→LIGHT）
- **原生层补强**：CatDBStatusBar 增加主线程强制、窗口未附加自动重试、lastDark 记录用户实际生效主题；MainActivity 在 onResume 与系统 uiMode 切换（onConfigurationChanged）时重放主题，回到前台/系统深浅切换后状态栏始终与页面一致，与 JS visibilitychange 幂等互补
- 版本号统一升至 2.2.11（versionCode 13）

## [2.2.10] - 2026-09-04

### Fixed（闪退加固 + 固定签名 + 文字图标占位）
- **冷启动状态栏原生设置全面 try/catch 加固**：CatDBStatusBar 与 MainActivity 所有入口异常兜底，任何机型/系统版本上的状态栏设置失败只记日志、绝不导致闪退；主题覆盖方案因与 Capacitor 库同名 style 资源合并冲突而放弃，冷启动颜色由原生插件在窗口就绪后设置
- **固定发布签名**：新增 android/catdb-release.jks（30 年有效期）并入库，本地与 GitHub Actions 共用同一密钥签名——此前 CI 每次全新 runner 自动生成不同 debug key，导致各版本签名不一致无法覆盖安装，现各版本可直接覆盖更新
- **启动器图标改为文字占位**：以橙色渐变圆角底 + 白色粗体「Typing」生成全套 legacy 与自适应图标（用户后续将专门设计图标）；manifest 同源图标引用同步更新
- 版本号统一升至 2.2.10（versionCode 12）

## [2.2.9] - 2026-09-04

### Fixed（冷启动状态栏修复）
- **冷启动状态栏设置可能静默失效**：CatDBStatusBar.applyForSystemTheme 原先经由 getActivity()（bridge 获取）取窗口，在 MainActivity.onCreate 早期 bridge 尚未就绪时会返回 null 导致设置被跳过。改为直接使用传入的 Activity，冷启动按系统夜间模式正确设置状态栏图标明暗与背景色（styles.xml 之外的保险）；顺带清理未使用 import
- 版本号统一升至 2.2.9（versionCode 11）

## [2.2.8] - 2026-09-04

### Fixed（状态栏原生直控 + 图标矢量重绘 + PWA 主题色）
- **状态栏图标明暗在 Android 15 上仍不可靠**：@capacitor/status-bar 6.0.3 走 `setSystemUiVisibility` 旧 API，在 Android 15 系统上被忽略。新增原生插件 `CatDBStatusBar`（WindowInsetsControllerCompat 直控状态栏/导航栏图标明暗与背景色），注册进 MainActivity 并在冷启动时按系统夜间模式设置一次；JS 主题切换改为优先调用该插件（官方 StatusBar 插件保留为兑底）
- **安卓图标仍看不清**：放弃放大 128px 复杂原图（持手机+卡片）的旧方案，改用矢量式重绘——圆角米色底 + 三角尖耳橙猫 + 深棕粗描边 + 大眼高光，无细碎细节；legacy 全套与自适应前景按 66% 安全区重新生成，48px 起各尺寸清晰可辨
- **PWA 状态栏/浏览器 UI 颜色与主题脱节**：index.html 此前未声明 manifest 与 theme-color。新增 manifest 链接与 theme-color meta（浅色 #F9F5F0），主题切换时动态更新（深色 #14100D）；manifest theme_color/background_color 由不匹配的深棕改为浅色主题色
- **顶部安全区**：body 顶部预留 `env(safe-area-inset-top)`，避免刘海/全屏形态下内容与状态栏重叠；html/body margin 归零（通配已含）
- 版本号统一升至 2.2.8（versionCode 10）

## [2.2.7] - 2026-09-04

### Fixed（安卓状态栏/主题/图标专项）
- **安卓状态栏与页面同色导致看不清**：根因是 Capacitor 运行时（@capacitor/core 与 status-bar 插件的 JS 代理）从未打进页面，`Capacitor.Plugins.StatusBar` 在 App 里不存在，运行时主题设置全部静默失效。修复：将 Capacitor 运行时与状态栏插件代理以纯 HTML 脚本方式内置到 `www/vendor`，并仅在原生环境下调用；页面加载完成 / 回到前台等多个时机重复同步，深浅主题下的状态栏底色与图标明暗正确切换
- **冷启动即正确**：新增 values-night 深色启动主题（深棕底 + 浅色图标），浅色启动主题显式声明深色图标（windowLightStatusBar），App 启动瞬间状态栏即与主题一致、可读
- **设置页主题行配色错乱**：外观主题下拉与电脑地址输入框原先硬编码白底 + 主题色文字（深色下浅字白底几乎不可见）。改为主题感知样式类，深浅两套配色与整体一致
- **安卓图标看不清**：以原始 6-phone-cat 图标副本重新生成全套：给小猫加一圈深棕描边、渐变底提对比、legacy 足幅 + 自适应图标内容收敛进 66dp 安全区、1024 主画布逐密度下采样。48px 起各尺寸均可清晰辨认
- 浏览器访问手机页时 /vendor 静态资源可正常加载（服务端新增 vendor 路由）
- 版本号统一升至 2.2.7（versionCode 9）

## [2.2.6] - 2026-09-04

### Fixed（内存与连接健壮性专项审计）
- **服务端 WS 心跳检测**：WebSocket 启用 heartbeat=30s，手机静默断线（杀进程/切网/休眠）后服务端能在约 1 分钟内回收僵尸连接，避免 ws 对象、协程与客户端记录长期滞留
- **手机端保活心跳 + 重连退避**：每 20s 发送心跳、60s 无响应自动断开重连，及时识别半开死链；断线重连改为指数退避（2s 起、上限 15s），电脑离线时不再每 2s 空转
- **大图粘贴内存优化**：剪贴板写入由 rows 列表+join（两份全图拷贝）改为单块 bytearray 逐行累积，峰值内存减半，避免低内存设备在 4K 图片粘贴时内存尖峰
- 版本号统一升至 2.2.6（versionCode 8）

### 审计结论（无需修改项）
- 服务端：连接清理（connected_clients / client_configs 随断开即移除）、日志环形缓冲（maxlen=400）、服务启停单例化且清理 runner/热键监听/zeroconf，均无泄漏
- 桌面端：日志与二维码轮询均为定界重绘/替换，无 DOM 累积；定时器全部单例
- 手机端：输入防抖、自动清空、打字动画、图片状态提示等定时器均单例且清理；图片链路无 objectURL 需 revoke；局域网扫描探测全部带超时中止与连接释放

## [2.2.5] - 2026-09-04

### Added
- **安卓局域网扫描（自动发现电脑）**：「设置 → 局域网扫描」一键扫描当前 Wi-Fi 局域网（HTTP 探测 5000-5010 端口），自动发现运行着 CatDB 客户端的电脑，显示 IP:端口与版本，运行中的电脑优先置顶高亮；点击结果一键填入地址并连接；可手动刷新
- **桌面端深浅色一键切换**：顶栏设置齿轮旁新增同款风格主题按钮（☀️/🌙），手动切换深色/浅色；首次默认跟随系统并记忆上次选择（与手机端主题相互独立）

### Changed
- **桌面端手机输入内容持久显示**：手机输入内容实时同步到桌面底部运行日志栏头部；停止输入后内容**不再自动消失**，持续显示到最后一次输入的内容，仅退出客户端才清除；空闲时圆点停止闪烁
- **安卓主页去掉红色 📷 角标**（与「点击小猫本体上传图片」重复），保留点击小猫选图上传与右下角圆形拍照按钮
- **界面元素重叠修复**：主页底部信息条（已同步 N 字）不再与右下角拍照按钮重叠；输入区最后一行文字不再被两个圆形按钮遮挡（新增底部留白）；帮助气泡与小猫图形间距调整，浅色/深色主题均无重叠
- 局域网扫描直接探测全部候选端口（不做“仅 5000 端口”筛选），避免电脑 5000 被占用、实际服务在 5001+ 时漏扫
- 版本号统一升至 2.2.5（versionCode 7）

## [2.2.4] - 2026-09-04

### Changed
- **安卓主页大幅简化**：移除主页的扫码连接条、地址栏与图片上传按钮；连接地址收进「设置」，点小猫头像=从相册选图发到电脑，右侧新增圆形拍照按钮（与左侧触控板按钮对称同款）
- **拍照按钮**：主页右下角圆形 📷 按钮直接调起相机/选图，拍到的照片经 WebSocket 粘贴到电脑光标处
- **触控板返回逻辑修复**：触控板页面新增「返回输入」按钮，可一键可靠回到输入界面（两个圆形按钮随之隐藏/恢复）
- **电脑地址输入**：在设置中填写即保存、重新打开不变，除非手动修改否则永不被覆盖
- **桌面端实时输入预览**：手机端打字时桌面窗口实时同步显示「手机正在输入：…」，停止输入数秒后自动消失
- **APP/EXE 图标**：改用上游原始项目的 6-phone-cat 图标（橙猫），同时应用于安卓 mipmap 全套与 Windows EXE
- 移除已废弃的扫码连接（jsQR）代码与依赖
- 版本号统一升至 2.2.4（versionCode 6）

## [2.2.3] - 2026-09-03

### Added
- **安卓深浅色主题**：设置中可切换 浅色 / 深色 / 跟随系统，默认跟随系统；页面背景、卡片、文字、输入框、小猫插画随主题适配，状态栏颜色与主题联动（不再是纯黑）
- **手机端扫码连接**：新增扫码入口，调起相机扫描电脑端二维码自动填入地址并连接；不可用时提示引导手动输入
- **手机图片发送到电脑**：输入框旁的图片按钮与点击中间小猫头像均可选择图片，通过 WebSocket 发送到电脑并直接粘贴到当前光标位置
- **小猫爪爪动画**：输入文字时中间小猫的两只爪爪随打字节奏摆动，停止输入后静止
- **APP 图标**：打包产物配有由原始小猫形象生成的图标（Android mipmap 全套 + Windows EXE）

### Fixed
- **安卓电脑地址输入框无法修改**：修复编辑失效问题，保存后立即重连并校验格式，错误时明确提示
- **桌面端端口耗尽会卡死**：启动/运行中自动检查端口占用并顺延；范围内全部被占时明确报错并记日志，不再静默卡死

### Changed
- **桌面界面进一步精简**：默认几乎不显示说明文字，提示改为悬停/点击气泡出现；日志面板可折叠
- 版本号统一升至 2.2.3（versionCode 5）

## [2.2.2] - 2026-09-03

### Fixed
- **桌面客户端无法操作 / 无日志输出**：pywebview 的 JS 桥接在页面加载后才注入，此前页面启动时误判为“浏览器模式”，导致所有操作与轮询失效。现在每次调用都实时探测桥接，并在 `pywebviewready` 事件后立即刷新
- **EXE 无法打开桌面窗口**：pywebview 自带 WebView2/互操作原生 DLL 此前未打入 EXE；spec 现通过 `collect_dynamic_libs('webview')` 显式收集
- **无声故障难排查**：日志同时写入 `%APPDATA%/CatDB/catdb.log`；服务线程异常不再吞掉，会记日志并立即返回；窗口创建失败自动降级为浏览器 + 托盘模式
- **显示/连接错误的 IP**：IP 列表私网地址优先排序，避免 VPN/虚拟网卡地址（如 198.18.x.x）抢在前面导致手机连不上
- **安卓状态栏黑色**：补装 `@capacitor/status-bar` 插件（此前配置完全没生效），状态栏固定 #F9F5F0 纯色 + 深色图标，与页面背景一致；页面背景统一为同色
- 版本号统一升至 2.2.2（versionCode 4）

### Changed
- 桌面界面按苹果设计语言重构：毛玻璃顶栏、卡片化三栏、macOS 风格开关/弹窗、系统字体栈、柔和阴影圆角

## [2.2.0] - 2026-09-03

### Added
- 桌面 GUI 主窗口（pywebview），920×660 三栏布局
- 触控板模式开关（左侧控制面板，对接上游已有后端）
- 开机自启控制（齿轮菜单，状态持久化）
- Android 纯色状态栏（与页面背景一致，不覆盖内容）
- GitHub Actions 自动构建 Windows EXE + Android Release APK
- HTTP JSON API（/api/status、/api/logs、/api/action），供浏览器版桌面页使用

### Fixed
- **EXE 打开 404 / 页面打不开**：GUI 改为加载本地打包的 desktop.html（不再猜测端口访问 127.0.0.1:5000），并新增端口自动探测；彻底移除协程内阻塞式 Event.wait（实测会饿死 Windows Proactor 事件循环导致全部 HTTP 超时）
- **APK 未签名无法安装**：release 构建类型固定使用 debug 签名（signingConfig signingConfigs.debug），CI 增加 apksigner 校验步骤
- **安卓 App 连不上电脑**：原 App 内置页面把 localhost 当成电脑地址；新增「电脑地址」手动填写（设置弹窗），并放行明文流量（usesCleartextTraffic）
- 移除 server.py 内嵌重复手机页（HTML_PAGE），统一以 www/index.html 为唯一事实来源

### Changed
- 主程序支持 `--minimized` 静默启动模式
- 桌面页重写：flex 流式三栏布局（25%/自适应/30%），状态灯、二维码 5s 自刷新、IP 标签切换、深色终端日志面板、齿轮菜单（开机自启/最小化/退出）
- 触控板开关真实生效（后端过滤手机触控指令）并持久化到 %APPDATA%/CatDB/config.json
- 快捷键可在桌面端修改（F1–F12/Home/End 等），运行中即时重建监听

### Removed
- 无（完整保留上游所有功能）